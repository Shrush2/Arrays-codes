class Solution {
    public int majorityElement(int[] v) {
        int el =0;
        int cnt = 0;
        int n = v.length;

        for(int i=0;i<n;i++){
            if(cnt == 0){
                cnt = 1;
                el = v[i];
            }else if(el == v[i]){
                cnt++;
            }else{
                cnt--;
            }
        }

        cnt = 0;
        for(int i=0;i<n;i++){
            if(el == v[i]){
                cnt++;
            }
        }
        if(cnt > n/2){
            return el;
        }
        return -1;
    }
}




=======================2nd approach ===========================
import java.io.*;
import java.util.* ;

public class Solution {
	public static int findMajority(int[] arr, int n) {
		// Write your code here.
		 double cnts = Math.floor(n/2);

		Map<Integer,Integer> map = new HashMap<>();
		for(int i = 0; i < n; i++){
			map.put(arr[i],map.getOrDefault(arr[i],0)+1);
		}

		for(int i = 0; i < n; i++){
			if(map.get(arr[i]) > cnts){
				return arr[i];
			}
		}
		return -1;
	}
}

T.C => O(N)
S.C => O(N)
